# Core HTTP client

requests>=2.31.0

# Socket.IO client (used for CoinSwitch / CoinDCX WebSockets)

python-socketio[client]>=5.11.0

# Engine.IO (dependency, but safer to pin)

python-engineio>=4.8.0

# WebSocket transport

websocket-client>=1.7.0

# Optional: faster JSON parsing (recommended for real-time)

orjson>=3.9.0

# Optional: data handling / logs analysis

pandas>=2.2.0
